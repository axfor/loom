# 待实现

拿 [DESIGN.md](DESIGN.md) 的九层和 [SYNTAX.md](SYNTAX.md) 的规格，对照今天代码里真有的东西。

**已完成的**：6 种文档类型、递归（`as`，两个方向验证过）、点号路径、`move`、`promote` / `demote`、重叠检测、**选择器**。

---

## 按层的缺口

| 层 | 已有 | 缺 |
|---|---|---|
| **L1 数据模型** | 6 种类型；递归 `as` | 类型仍是 `ast.New` 里一条 switch，加类型要改编译器 |
| **L2 命名** | 名字、路径、派生地址、选择器、身份 | —— |
| **L3 变换** | 以上全部 + move/promote/demote/选择器/project | ~~split~~ ~~join~~ ~~wrap~~ ~~swap~~ 是糖；~~align~~ 已砍；`unwrap` 随全新语法实现 |
| **L4 保证** | 不变式、move 与层级的逐字节校验、重叠检测、保证量 | —— |
| **L5 组织** | `import`、`frontmatter` 策略 + 写回 | `fn`（跨文件复用仍无解，但最痛的一处已消） |
| **L6 语法** | 今天的语法 | **全新语法整套**（`Self:`、围栏、点号取名、`return`、`if`） |
| **L7 编译器** | anchor completion、类型/路径/缩进推断 | 参数类型推断、套用命中推断 |
| **L8 演进** | —— | **语言版本声明** |

---

## TODO

### 第一梯队 —— 全部了结

- [x] **选择器** ✅ `base.sections(match: "^Step ").demote()` / `base.sections(empty).drop(reason:)`
      用调用 + 具名参数落地。谓词 `match:`、`level: N`、`empty`，可叠加（互相收窄，不是取并）。
      `level:` 带来了词法层唯一的新增：数字字面量。它只对 markdown 标题成立，写在别的类型上当场报错——
      一个永远只能匹配到零个的谓词是笔误，不是查询。
      带选择的语句在核对前**展开成逐节点的语句**，所以保证、记账、报告都照常按名字工作。
- [x] **语言版本** ✅ `loom "1.0"`。声明比编译器新的版本会被拒绝并提示 `lm update`；不声明仍然可用。
- [x] ~~**`wrap`**~~ —— **不做**：`wrap(a, b)` ≡ `before(a)` + `after(b)`，不增加任何能力。
- [x] ~~**`swap`**~~ —— **不做**：≡ 两个 `move`（都对着来源解析，互不干扰）。
- [x] **`lm fmt`** ✅ **不做**——先修正过一次成本（约两百行打印器，不是「几乎免费」），这次量了收益，收益是零。

      **格式漂移实测为 0。** 81 个模板，三项都查了：

      | 查什么 | 结果 |
      |---|---|
      | 行首缩进不一致 | 0 |
      | 具名参数冒号后缺空格 | 0 |
      | 同一个名字既写标识符又写字符串 | 0 |

      原因不是大家自律，是**这个语言没给出多少可漂移的自由度**：一行一句，没有表达式嵌套，
      没有括号换行的选择。剩下的自由度只有语句间的空行和围栏的缩进，而多数模板一共 3–7 行。

      **它也不是机械改写的前提。** `build/identity.go` 的 `follow()`（跟随重命名改写锚点）走的是
      「词法 → 按 `Stmt.At` 定位记号 → 拼接字节区间」，不经过任何打印器。下划线迁移这类改写复用的是同一套机器。

      **什么会让它反转**：语言真的长出可漂移的自由度（多行表达式、嵌套块），或者出现多人协作的 diff 噪音。今天两样都没有。

### 第二梯队 —— 全部了结

- [x] **`project`** ✅ `base.start(base.sections(match: "^Step ").project(\`- {name}\`))`
      字段 `{name}` / `{level}` / `{body}` —— 全是文档真有的结构。
      ~~**`{anchor}` 决定不做**~~ —— **改了，已实现为 GitHub 的锚点规则。**
      原先的理由是「锚点是渲染器的约定，猜一个等于编一条验不了的规则」。那个顾虑靠**点名是谁的规则**来解决，
      而不是靠不做：GitHub 的算法是确定的、有文档的，产物 XSDD 就发在 GitHub 上。
      更要紧的是不做的**后果**：`{anchor}` 原样穿透进产物，成了指向 `#{anchor}` 的坏链接，构建一声不响。
      现在任何未知字段都在写的地方报错。
- [x] ~~**`align`**~~ ✅ **已决定：不做**。任何按位置配对的算子在上游删一节又加一节时会静默配错。
      让它可检测只有「与基线比对」一条路，而那就是身份（第三梯队）。它是伪装成算子的身份问题。
- [x] ~~**`split` / `join` / `unwrap`** ✅ **推敲完了，都不做**~~ —— **后来随全新语法全部实现**，见文末「全新语法」。
      下面是当时不做的理由，留作记录；`unwrap`/`join` 要 `reason:`、只在 markdown 上成立，正是从这里来的。

      **`split` 是糖。** 拆开一节 = 在拆点前插一个标题，而拆点本来就是可寻址的（子节点，或 `line()`）：

      ```
      base.Setup."Step A".before(`## Setup, part two`)
      ```

      实测产物正确，**保证仍是 100%**。和 ~~`wrap`~~ ~~`swap`~~ 同一把尺子。

      **`join` 归约到 `unwrap`。** 「与下一节并起来」= 去掉下一节的标题。相邻就是纯 `unwrap`，不相邻就是 `move` + `unwrap`。它自己不带任何新能力。

      **`unwrap` 是真缺口，但不该现在填。** 它今天确实写不出来，原因值得记下来：

      > **一节 markdown 就是它的标题。** 把标题行 `drop` 掉，编译器的身份模型里这一节就不存在了——
      > 于是记账报 `section "Setup" is not in the product`，哪怕正文一个字节都没动。
      > 这不是算子缺失，是身份的定义如此。

      绕路要付的代价实测过：整节 `drop`（连同子节共三条理由）+ 正文当作我们的内容重写回去，
      一个五节的文件从 **100% 掉到 18%**——上游原话变成了「我们写的」。

      不做的两个理由：

      1. **只有 markdown 讲得通。** 标题行删掉、子节各升一级，正文逐字节不变——可验证，和 `promote` 用同一套 `unlevel` 检查。
         但 unwrap 一个 shell 函数是把函数体反缩进到顶层，那**改变脚本的行为**（局部变量、return），编译器推理不了；
         unwrap 一个 yaml / toml / json 键是把子键提到上一层，路径和缩进全变。这两类不是保证能覆盖的变换。
         一个只对六分之一类型成立的算子，要么改名叫别的，要么等到确实需要。
      2. **真实树上零需求。** 81 个模板里 15 条 `drop`，**全部是整节**；`line()` 一次都没用过。
         「留正文、去标题」这个动作没有人做过，连绕路的痕迹都没有。

      **什么会让它反转**：出现「保留上游正文但产物里不该有这层标题」的真实需求，并且有人已经在用上面那条绕路付保证的代价。
      届时按「markdown 专属」实现，和 `level:` 一样在编译期拒绝用在别的类型上。
- [x] **保证量** ✅ 报告末行给出「上游有多少比例被证明逐字节不变」，低于 100% 的文件在自己那行注明。

### 第三梯队 —— 大工程

- [x] **身份**（L2.4）✅ `lm sync` 比对新旧两版上游，内容逐字相同而名字变了 = 重命名，改写模板里的锚点并报告。
      删除、歧义、内容也改了 —— 三种都不跟随。跨类型可用（markdown 节、shell 函数、yaml 键）。
- [x] ~~**全新语法**（L6）~~ —— ~~算完了，不做~~ **反转了，已实现**。见文末。
- [x] ~~**`fn` 与套用**（L5）~~ —— ~~依赖全新语法，随之不做~~ **随全新语法实现**：`fn` 在解析时内联；套用由 `frontmatter`/`keys`/`body` 设置推断。
- [x] **反向查询** ✅ `lm uses [path-fragment]`。把构建期已经算出的锚点解析反过来按上游节点分组；没有任何新计算。

---

## 已知要先解决的争议

来自 [DESIGN.md](DESIGN.md) 末尾的对抗审核。三条致命项**都已了结**，但了结的方式不一样：

1. ~~**`align` 能静默配错**~~ —— ✅ **砍掉**。见 SYNTAX §7.7。
2. ~~**`if !ok` 的唯一真实用途是猜**~~ —— ✅ **随新语法一起不做**。它与 L2.4 的身份方案解决同一问题且互相打架；
   身份已经实现（`lm sync` 跟随重命名），那条路走通了，猜的那条就不必走。
3. ~~**`err.format` 能进产物**~~ —— ✅ **同上**。`err` 是新语法的构造，新语法不做，这个洞就不存在。

第 2、3 条是被「不做新语法」这个决定一并了结的，不是被修掉的。**如果哪天新语法的结论反转，这两条要原样搬回来。**
新语法反转后搬回来核过：`if` 分支和捕获的结果在织之前对着上游判定，报告与织共用同一次判定；`err.format` 只进构建的报错，进不了产物。

两条「承诺过强」：`move` 的「字节多重集不变」已在实现时降为「该部分逐字节不变」并落地；`project` 的 slug 问题已按 GitHub 的规则了结（`{anchor}`）。

---

## 全新语法的迁移成本（对抗审核 #8 的答案）

拿真实的 81 个模板测算，不是估计。

### 模板侧：几乎全是机械转换

| 构造 | 数量 | 变成什么 | 机械？ |
|---|---|---|---|
| 标识符形式带下划线 `base.How_Skills_Work` | **79** | `base."How Skills Work"` —— 下划线不再代空格，须解析成真名 | ✅ 编译器知道真名 |
| 裸字符串参数 `after("X")` | **185** | `after(self."X")` —— 语义变了，字符串现在是字面量 | ✅ |
| 字符串形式名字 `base."A B"` | 66 | 不变 | ✅ |
| `base.replace(self, reason: "r")` | 5 | `return self // reason: r` | ✅ |
| **`base.merge(self)`** | **9** | `return self // reason: ?` —— **今天没有理由可搬** | ❌ **要人写 9 条理由** |

**269 处机械 + 9 处要人判断。** 写一个转换器（编译器已经能解析旧语法、知道每个锚点的真名和字节位置——`follow()` 刚证明了这套机器可用），跑完之后人只需要补 9 条 reason。

### 内容侧：三分之一今天就该内联

80 个参与编织的内容文件，**中位数 94 行**（平均 129 被一个 2110 行的文件拉高）：

```
 0-20 行  █████████ 9
21-50 行  ██████████████████ 18      ≤50 行：27 个 = 33%
51-100 行 ████████████████████ 20
101-200 行 ███████████████████████ 23
201-500 行 ████████ 8
501+ 行   ██ 2
```

**33% 的文件短到内联毫无疑问**，再往上 25% 在 51–100 行之间，取决于编辑器支持。

**一处更正。** 这份估算先前写的是「内容一个字都不用搬，`Self:` 用不上」。那个结论错了，错在两处：

1. **用了平均数**。中位数 94、且三分之一在 50 行以下，分布和平均数说的不是一回事。
2. **把自己发明的判据当成了测量**。「几十行以上就该外置」是我在 SYNTAX §6.5 写的，理由是「留在真实文件里才有 markdown 预览和 lint」——**而那是可修的工具缺口，不是设计的固有代价**。扩展今天已经在给宿主语言注入 `{{@name}}` 的语法；给围栏注入 markdown / shell / json 高亮是同一套机制。修好之后，能内联的上限会高得多。

所以：**两种形态都要，按产物选**——这本来就是 SYNTAX §6.2 说的。长文件走 `import`，短文件内联。真实数据上，至少三分之一属于后者。

### 编译器侧

| 要重写 | 行数 |
|---|---|
| `lang/objparse.go` | 1018 |
| `lang/lexer.go` | 208 |
| `lang/template.go` | 98 |
| 编辑器镜像 `lib/loom.js` + grammar | 996 |
| **合计** | **约 2300 行** |

外加：转换器（约 200 行）、双语法并存期（旧的要能读到转换完成）、全套测试重写。

### 判断

**模板侧几乎免费，编译器侧约 2300 行重写。**

而这 2300 行买到的是什么，值得直说：

| 新语法带来的 | 在这棵树上的实际收益 |
|---|---|
| **一个产物一个文件** | **至少三分之一的产物**（27/80 内容 ≤50 行），编辑器支持跟上后更多 |
| **`Self:` 资源段** | 同上——它就是这件事的实现 |
| `fn` 复用 | 消掉 23% 样板（68/291 条语句），但**须跨文件才有效** |
| `return` 取代 merge/replace | 少一个概念 |
| 点号原样匹配 | 消掉「下划线代空格」这条魔法 |

**收益不集中在一项。** 一个产物一个文件对至少三分之一的树是真实收益，而且它是这套语法最初要解决的那件事。

### 前提：编辑器要先能在围栏里做语法高亮

内联的上限由这个决定，而它是**可修的**：扩展今天已经给宿主语言注入 `{{@name}}`，给围栏注入 markdown / shell / json 是同一套机制。

> **建议顺序**：
> 1. ~~先做**围栏语法注入**~~ ✅ **已完成**：围栏字面量进了今天的语法（词法层），扩展按标签注入 markdown / sh / json / yaml / toml / js 的高亮。
>    **内容现在可以写在今天的 `.lm` 里**，不需要全新语法。
> 2. ~~再做 `fn`~~ ✅ **换了做法并完成**：那 23% 样板是**跨文件**的（一行在 37 个模板里），文件内的 `fn` 对它零收益。
>    改为 `frontmatter` 设置 + 编译器写回模板——说一次，37 处不用再抄。
> 3. ~~全新语法最后~~ —— **算完了，见下。**

---

## 全新语法：已按指示全量实现

先前这一节的结论是「建议不做」——理由是五项收益里四项已增量拿到，只剩 `return` 一个关键字，对不起 2300 行重写。
**作者指示全量实现，已完成。**

实现方式和原估算不同，代价也因此小得多：**没有重写,是在今天的语言上增量长出来的**。

| 构造 | 状态 | 怎么做的 |
|---|---|---|
| 方括号谓词 `[level == 2 && name ~ "^S"]` | ✅ | 词法加 `[ ] && \|\| ! == != < <= > >= ~`；谓词是一棵小树，不是表达式语言 |
| 轴 `children / next / prev / parent / first / last` | ✅ | 在 resolve 期展开成具名语句，所以记账、校验、报告照常按名字工作 |
| `if cond { } else { }` | ✅ | 问的是**上游文档**，只读；weave 与报告共用同一次 resolve |
| 结果捕获 `ok = ...` / `if !ok` | ✅ | 捕获是失败唯一的出口，所以捕了不用是错误 |
| `return self` / `return err.format(...)` / `return` | ✅ | `return self` 就是整档替换；`err.format` 只进消息，不进产物 |
| `fn`（带参数） | ✅ | **解析期内联**，所以构建期没有调用栈，也不可能递归 |
| `Self:` / `Self as name:` 资源段 | ✅ | 围栏的语言标签就是种类；地址 `self[.段][.种类].部分`，省略即推断，推断不出唯一解报错 |
| `wrap` `swap` `unwrap` `split` `join` | ✅ | wrap 是糖；swap 是一次交换而非两次 move；unwrap/join 要理由；split 插入的标题是我们的，戴标记 |
| `.values` `.count` | ✅ | `values` 与 `keys` 指同一批节点；`count` 在条件里与 `any` 同义 |
| `base = "path"` | ✅ | 上游改过路径 |
| `project` 块形式 | ✅ | 与括号形式同义 |

### 对照附录 A：八处已了结

规格附录 A 把每个构件用了一遍。拿它当清单跑过一遍，当时八处不接受；**七处已实现，一处是有意不做**。

| # | 规格写法 | 状态 |
|---|---|---|
| 1 | `base.sections[has["X"]]` | ✅ 两种写法同义，`has."X"` 也留着 |
| 2 | `base.sections[level == 2].first.children` | ✅ 轴可以接着走，每步按「这一步落在一个还是一组上」校验 |
| 3 | `base.A.move(base.C.after)` | ✅ 与 `move(after: base.C)` 同义 |
| 4 | `base.A.split(在哪)` 单参数 | ✅ 拆点是一个标题时，它升到被拆那一节的级别；两参数形式插入一个戴标记的新标题 |
| 5 | `base.A.join()` 不带理由 | ❌ **有意**：它删掉上游一个标题，和 `unwrap` 同类，要 `reason:` |
| 6 | `...description.end(x)` | ✅ 与 `.append(x)` 同义 |
| 7 | `base.start.project(sel){ ... }` | ✅ 位置词成了派生地址，`project` 成了写在它上面的方法 |
| 8 | `base.sections` 不带谓词 | ✅ **裁决：裸类别名 = 全部**；`base.sections()` 空调用仍报错 |

第 8 条的两条规则不再打架：**裸词是明确的意图**（「全部」），**空调用像忘了填**（那才是笔误）。

### §10 的类型检查 ✅ 已实现

规格 §10：「写操作要求内容的类型与落点相符，**不符在解析期就报错**」。已落地。

```go
import r "/run.sh"
base.Overview.after(r.boot)
                    ~~~~~~~ shell 写不进 markdown
```

依据是实的：插进去那段没有标题，按名字记账的机器看不见它——上游完整性仍然成立，
但**我们自己的那段内容不在任何账上**。

两处必须放行，都是实测真实树撞出来的：

1. **视图里的自己**。`base.prompt.as(markdown).append(self.body)` 里的 `self.body` 是
   我们 prompt 里的那份 markdown，不是外面那层 toml。显式点名的文件不在此列——它仍然是扩展名说的那种。
2. **标签只是标签时**。围栏的 `sh` / `bash` / `yml` / `md` 映射到语言认识的类型，照查；
   `python` / `js` / `nope` 语言没有对应类型，那是给读者和编辑器看的标签，无从检查。

真实树 81 个模板全过，产物零差异。

### 正文与文法逐条核过，补了两处

附录只是摘要。正文 §0–§14 的 81 段代码和 §11 的 EBNF 逐条跑过之后，补了两处：

| 缺口 | 规格在哪说的 | 问题 |
|---|---|---|
| **`value` 谓词** | §4.2 的例子 `base.keys[value == ""]`（下面的表里漏了它） | 没实现。而且和 `empty` 不是一回事：`a = 1` 和 `b = ""` 在「下面有没有内容」上都算空，只有 `value` 分得开 |
| **`lines` 作为类别** | §11 文法的 `class` 里列着 | 行**能按名字找到**（`line("...")`）却**枚举不出来**，所以 `base.lines` 这个组永远是空的 |

行现在是节点，名字就是它说的那句话——和 `Find` 一直匹配的东西一致。**空行没有名字**，所以选不到它：
否则一个文件里会塞满一堆同名节点，任何锚点都能匹配到任何一个。

正文里另外 10 处报错都是探针的问题，不是缺口：`fn` / `import` / 资源段在顶层解析，我喂给了
`p.node()`；还有几行是图示和只隔一个空格的说明文字。逐个核过。

### 规格已全部实现

先前记为「有意偏离」的五条，重审之后三条该做、两条该说清楚。

**三条已实现**，破坏性的那两条放在 `loom "2.0"` 版本门后——已经写下的树不会在脚下改变意思：

| 构造 | `loom "1.0"` 或不声明 | `loom "2.0"` |
|---|---|---|
| `base.How_Skills_Work` | 下划线代空格，匹配「How Skills Work」 | 原样匹配名字 `How_Skills_Work` |
| `after("X")` | 指我们叫 X 的那一节 | 就是这行字 |

第三条是 **`split` 单参数**——我先前读错了规格。切点 `base.Setup.children.first` **本身就是一个标题**，
第二半不需要新名字：把那个子节提到父级即可。两参数形式留着，用于切在非标题处。

**两条说清楚：**

1. **`join()` 要 `reason:`** —— 这不是偏离，是**规格自相矛盾**。§7.2 把 join 归为结构变换（不需要理由），
   §12 又要求「上游内容不见了且无人认领」必须报错。join 删掉的是上游一个标题，名字确实消失了。
   而且 join 的实现**就是**对下一节做 unwrap，而 §7.3 要求 unwrap 给理由——
   同一个操作因为拼写不同而一个要理由一个不要，比两条规则里的任何一条都糟。实现选了保住更强的那条。

2. **`Self:` 里的 `#`** —— 规格的笔误，例子已改。markdown 的 h1 是**文档的标题**，不是其中一节；
   若把它当节点，每一节都成了它的子节点，「删掉标题」就等于「删掉整个文件」。
   实测过：全局放开 h1 之后真实树报一条错，而那条错**是对的**（README 的标题被整份换掉，从没记过账），
   但要交代它就得连同它下面全部五节一起交代——代价与收益完全不成比例。
   另一条路「只在资源段里放开 h1」会造成「内容从资源段挪进文件就换了一套寻址」的不一致。
   被织的 63 个 markdown 里**没有一个**只有 h1，所以这条限制真实但无暴露面。

### 为什么代价远低于原估算

原估算假设要重写 `objparse.go`(1018) + `lexer.go`(208) + `template.go`(98) + 编辑器镜像(996)。
实际是**加**：新构造各有自己的文件（`lang/newparse.go`、`lang/newinterp.go`、`build/resolve.go`），
旧语法一行没动，真实树 81 个模板产物零差异。

那份估算错在把「新语法」当成一个必须整体替换的东西。除了上面那两条破坏性的，其余每一条都能单独加。

---

## 还开着的

设计阶段列出的条目**全部了结了**——建成、砍掉、或归约成已有的东西。剩下的是这几条，都不是设计缺口：

| 开着的 | 性质 | 现在的判断 |
|---|---|---|
| ~~**废弃下划线代空格**~~ | ✅ **已定：不废弃** | 规则保留。churn 按这个决定收口：编辑器改成跟编译器一样写下划线形式 |
| ~~**`project` 的 slug**~~ | ✅ **已实现** | `{anchor}` 按 GitHub 的规则生成，未知字段在写的地方报错 |
| **加文档类型要改编译器**（L1） | 已知限制 | `ast.New` 是一条 switch。六种类型今天够用，没有第七种在等 |
| ~~**参数类型推断、套用命中推断**（L7）~~ | ✅ **已了结** | `fn` 内联后每个调用点按实参检查；套用由 `keys`/`body` 设置推断 |
| ~~**`unwrap`**~~ | ✅ **已实现** | 要 `reason:`，只在 markdown 上成立 |
| **text 类型的丢失检查** | 待你定夺 | 见下方 |

每一条都写了「什么会让这个结论反转」。**不写反转条件的结论不算结论**，因为那样下次只能重新吵一遍。

---

## 下划线代空格：保留（已定）

给编辑器做镜像差分时撞见的问题:**两半工具链对同一个名字写法不同，而且互相覆盖。**

决定是**保留下划线规则**，所以 churn 往编译器这边收口——编辑器改成写编译器写的形式，而不是反过来。

编译器自己分两种写法，实测确认过，编辑器现在照做：

| 位置 | 谁写 | 形式 |
|---|---|---|
| **上游锚点** `base.X` | `lm sync` 的 `follow()` → `NameText` | `base.How_it_compares` |
| **我们的内容** `self.X`、字符串参数 | anchor completion → `Quote` | `self."Our Note"` |

分工是有道理的：上游锚点那个记号**会被 sync 重写**，所以两边必须一致；我们的内容是构建期机械追加的，加引号永远无歧义。

一处**有意保留的不一致**：编辑器还会给 `section` / `key` / `line` 这些种类名加引号，编译器不会。
实测 `base.section` 对编译器不歧义（种类只以调用形式出现），但加引号零成本，省掉读者的一次疑问。
差分测试把这条单独断言，免得它从「决定」退化成「漂移」。

---

## text 类型的「丢失检查」：一条要你定的

「上游内容不得静默丢失」这条检查，今天覆盖 markdown 节、shell 函数、toml/yaml 键——**凡是编译器能按名字枚举的节点**。
`text` 不在其中，因为它的节点是**行**。

真实树上这不是假设：`scripts/validate-skills.js` 是 `merge`，走的正是最容易丢东西的那条路。
量过了：上游 70 行里 **8 行不在产物中**，全部来自我们重写过的那段注释。

**我没有顺手把它加上，因为这条规则对 text 不成立。**

| 类型 | 检查的是 | 改内容算不算丢 |
|---|---|---|
| markdown / shell / toml / yaml | **结构**：这个名字还在不在 | 不算——节还在，正文随便改 |
| text | 只能是**行**，而一行就是它的内容 | **算**——改一行等于丢一行 |

也就是说，给 text 加上这条不是「同一条不变式的推广」，是**引入一条严格得多的新不变式**：
merge 里每改一行都要写一条理由。那 8 行就是代价。

三条路：

1. **加上** —— 你的构建会立刻报 8 个错，逐条补 `reason` 或把行留回去。之后 text 和其他类型一样安全
2. **不加** —— text 的 merge 继续可能静默丢内容，`.js` 那两个文件是现实暴露面
3. **加成警告** —— 不阻断构建，只在报告里列出。但这违背「lost 必须为 0」的一贯态度

我倾向 **1**，因为静默丢失正是这门语言存在的理由；但那 8 个错会落在你的树上，所以是你的决定。
