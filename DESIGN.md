# Loom 设计

> 把散落在各种类型和位置的**文档与数据**，组织成一张结构化、**可引用**的布。

下面从这句话推出需要的全部机器。不参照现有实现；实现放在最后，作为这套系统的一个实例。

---

## 一、两个数据概念

**文档** = （种类，文本）。
**部分** = （名字，跨度）。种类决定一份文档有哪些部分；名字是**作者写的**（标题、函数名、键名），不由我们发明。

一条递归规则把这两个概念变成一张布：

> **一个部分的内容，本身可以作为文档打开。**

一个 json 值里是一段 markdown，那段 markdown 有节；一个 markdown 代码块里是 shell，那段 shell 有函数。

所以「混杂」不是「很多种文件堆在一起」，是**种类套着种类**。这张布有深度。数据和文档在这里没有区别——json 是文档，它的值也是文档。

---

## 二、一个地址

**地址** = 从某份文档出发的一串名字。

```
d.a.b.c
```

因为部分可以作为文档打开，这串名字**自然跨种类**，不需要第二套语法。

### 派生地址

除了部分本身，四个位置也能被寻址——它们是部分边界上的**空跨度**：

```
d.x 之前   d.x 之后   d 的开头   d 的结尾
```

这一步是整个系统里最省事的一刀：**「在 X 之后插入」不是一个操作，是一个地址。**

---

## 三、一个操作

```
write(地址, 文本)
```

把地址指向的跨度，换成给定的文本。**没有第二个操作。**

今天语言里的八个动词，全部是它在不同地址上的实例：

| 写成 | 其实是 |
|---|---|
| `after(t)` / `before(t)` | 写到边界空跨度 |
| `start(t)` / `append(t)` | 写到文档首 / 尾的空跨度 |
| `replace(t)` | 写到该部分的跨度 |
| `drop()` | 往该部分的跨度写空 |
| `set(v)` | 写到值的跨度 |
| `merge(d)` | 写到**根**跨度 |

`as` 不是操作，是地址派生：把一个部分当作文档打开。

### 语法不用重做

值得注意的是：`base.Overview.after("...")` 里，**动词是地址的一部分，不是操作**。`base.Overview.after` 读作「Overview 之后那个空跨度」，括号里才是要写的文本。

也就是说今天的语法**已经在表达这套系统**了，只是从没这么描述过。把它改成 `base.Overview.after = "..."` 之类只是换个写法，不增加任何表达力。**这套系统要求的语法变化为零**——真正要加的只有抽象。

---

## 四、一个不变式

既然唯一的改动方式是「把某段跨度换掉」，那么：

> **写集合之外的每一个字节，与来源逐字节相同。**

这不需要检查，它是构造的结果。要验证也只需一步：**把写过的跨度抠掉，剩下的必须等于来源。**

标记（`<!-- MINE:BEGIN -->`）不是注释风格问题——**它就是写集合在产物里的记录**，让「抠掉」这件事在事后仍然可做。

### 保证是量，不是档

一个视图的可信程度 = **产物中不在写集合内的比例**。

| 写集合 | 上游被保证的部分 |
|---|---|
| 几个空跨度（纯插入） | 100% |
| 若干部分跨度（replace / drop） | 除这些部分外全部 |
| 根跨度（merge、整文件替换） | 0% |

所以 `merge` 不是"另一类东西"，它是写集合等于全文的那个极端。它必须写明理由、必须在报告里单列——不是因为它特殊，而是因为它的保证量是零。

**这条给了未来任何扩展一把尺子**：不问「允不允许」，问「它的写集合有多大，报告里说清楚了没有」。

---

## 五、引用与图

**读**一个地址 = 取出那份文档。引用就是被当作值使用的地址，它**不复制**。

写只发生在写集合，读发生在任何地方。于是：

> **把构建期发生过的读记录下来，依赖图就有了，不需要额外机制。**

「谁引用了这一节」「它移走会炸掉什么」「这个产物由什么组成」——都是这张记录的反向查询。这不是新功能，是**不把已经算出来的东西扔掉**。

---

## 六、身份

名字来自文本。文本变了，名字就变了。

所以**身份不是一份文档的属性，是两个版本之间的一个关系**。

这条推论决定了它只能住在哪里：**只有同时握有新旧两版的那一刻，才能判断「内容没变而标题变了 = 重命名」而不是「删除 + 新增」。**

于是三条路直接出局：

| 方案 | 为什么不行 |
|---|---|
| 给上游加 id | 上游不可碰是前提 |
| 内容哈希 | 把「改标题就断」换成「改内容就断」 |
| 模糊匹配 | 猜错的产物和正确的长得一模一样 |

唯一的位置是**取上游新版的那一刻**。那时把重命名算出来、记下来、**报告出来**——引用跟随是被声明的，不是被猜的。

---

## 七、种类

一个种类要进这张布，回答三个问题：

1. 它把文本切成哪些**有名字的部分**？
2. 它的部分能被当作文档**打开**吗？（往下嵌）
3. 它能作为别人的部分**被打开**吗？（被嵌）

只答第 1 问的种类只做了三分之一——它只能当顶层文件，进不了那张有深度的布。

---

## 八、抽象与融合

### 判据由系统给出

一条规则要能只说一次。什么样的抽象是安全的，不需要争论：

> **展开之后，是不是仍然只有 `write(地址, 文本)`？**

这等价于说：**抽象的返回类型是「一组写」，不是「一段文本」。**

返回文本的函数（`func merge(){ return a.call(...) }`）不是被禁止，是它的写集合无法界定——等价于写了根跨度，保证量归零。它必须和 `merge` 走同一条路：写明理由、报告单列。**不是禁止计算，是计算的代价在报告上标价。**

### 全部在 `*.lm` 里

规则用语言自己写，不进设置文件——设置是一行一条，装不下块，而语言的东西该待在语言的文件里。

```
// mine/rules.lm —— 定义规则，自己不产出任何文件
rule skill {
  base.frontmatter.description.start(self.frontmatter.description)
  base.append(self.body)
}

apply skill to "skills/**"
```

两点设计：

**规则体就是语句本身。** 不发明新语法——`rule` 块里写的和模板里写的是同一种东西，因为展开后必须是同一种东西。

**按模式套用，不是逐个引用。** 如果每个模板都要写 `import` + `use`，37 处重复就变成 74 处。`apply ... to` 让那 24 个「除了样板没有一句自己的话」的模板**整个消失**。

一个只有 `rule` / `apply`、不指向任何产物的 `.lm`，就是规则文件；构建报告里单列一行说它套用到了哪些产物。

### 融合：是并集，不是优先级

这是整套系统最省事的一处，而且是白送的。

因为**所有地址都对着来源解析**，编辑收集齐了再统一应用，所以：

> **规则的写 和 模板的写，融合方式就是集合并。**

不需要定义谁覆盖谁、谁先谁后——不相交的写**可交换**。只有两种情况需要说清：

| 情形 | 语义 |
|---|---|
| 两个写落在**同一个空跨度**（都往 X 之后插） | 都生效，按顺序排：规则在前，模板在后 |
| 两个写覆盖**相交的非空跨度**（一个 replace 一个 drop） | **冲突，报错**，并指出是哪条规则和哪条语句 |

所以「怎么融合」在这套系统里不是一个需要设计优先级的问题，而是一个**可检测的重叠问题**——而检测重叠用的正是维护不变式的同一套机器。

### 拿真实数据验过

把上面那条 `rule skill` 套到唯一使用 Loom 的项目的 81 个模板上：

| 结果 | 数 |
|---|---|
| 命中规则的模板 | 37 |
| 套用后**整个文件消失**（没有剩下任何自己的话） | **16** |
| 套用后只剩 1–2 条 | 14 |
| **真正的重叠冲突** | **0** |

「融合是并集」这条在 81 个真实模板上**一个反例都没有**。

这不等于它永远不会冲突。它说明的是：**冲突是罕见的例外，所以值得报错，而不值得为它设计一套优先级规则**——而这正是上面那张表假设的东西。设计在写代码之前就被真实数据验过一次。

### 退出

个别文件不想要某条规则时，必须**明说**，否则就是上面那个重叠错误：

```
skip skill
```

明说了就允许，并在报告里记一笔。沉默地不生效是不行的——那正是这门语言从一开始就在防的东西。

## 九、语法

### 9.0 原则：作者写最少，其余归编译器

这门语言已经有一个最纯粹的例子：**anchor completion**。你在自己的 `.md` 里加一节，构建算出它该插在哪、并把语句写回模板。作者一个字没写。

把这条推到底，就得到语法的取舍标准：

> **凡是编译器能算出来的，作者就不该写；凡是编译器不该猜的，作者必须写。**

两句都要。前一句给开发体验，后一句是这门语言的底线——**算得出**和**猜**的分界线，就是语法该不该出现的分界线。

---

### 9.1 地址

```
base                       上游那份文档
base.Overview              它的一个部分（名字是作者写的标题）
base."How it compares"     带标点的名字用字符串形式
base.How_it_compares       标识符形式：_ 匹配空格或下划线
base."A"."B"               路径：在 A 的整章内定位 B
base.frontmatter.description   值
base.prompt.as(markdown).Steps 打开一层：值里的文档的部分
```

**编译器补的**：种类（由扩展名定）、部分的类别（`Overview` 在 markdown 里默认是节，在 shell 里默认是函数）、以及**路径只在必要时才需要**——名字唯一时不写路径。

**编译器拒绝的**：名字匹配到两个部分。它不挑第一个，它停下来，让你写路径。

---

### 9.2 写

地址加一个**位置词**，就指向一段跨度。括号里是要写进去的东西。

```
base.Overview.after(...)     Overview 之后的空跨度
base.Overview.before(...)    之前
base.start(...)              文档开头
base.append(...)             文档结尾
base.Overview.replace(...)   Overview 自己那段
base.Overview.drop()         往它那段写空
base.frontmatter.description.set(...)   值那段
base.merge(self)             根那段
```

位置词属于地址，不是八个不同的操作——这是第三节推出来的。语法上它就是地址的最后一步。

---

### 9.3 内容从哪来

```
base.Overview.after("Where this fits")      我们文件里的同名部分
base.Overview.after(self."Where this fits") 同上，写全
base.Overview.after(cmd.body)               另一份我们的文件
base.Overview.after(a, b, c)                多个，按序
```

裸字符串就是「我们这份文件里叫这个名字的部分」——最常见的情况写得最短。

---

### 9.4 块：缩进定界，不需要转义

反引号字面量装不下真实文档：第一个行内反引号就终止它。真实的 markdown 里全是 `` `code` `` 和代码块。

解法是**缩进定界**：

```
base.Overview.after:
    ## Where this fits

    Run `lm build` to weave it.

    ```sh
    echo hello
    ```
```

冒号之后、缩进之内的全部是内容。**没有终止符要选，没有字符要转义，反引号和代码块都只是普通文本。** 公共缩进由编译器剥掉（今天的字面量已经这么做了）。

这正是「复杂留给编译器」：作者只做了一件他本来就会做的事——缩进。

---

### 9.5 规则

```
// mine/rules.lm —— 自己不产出任何文件
rule skill {
  base.frontmatter.description.start(self.frontmatter.description)
  base.append(self.body)
}

apply skill to "skills/**"
```

规则体里写的和模板里写的**是同一种东西**——因为展开后必须是同一种东西。不发明第二套语法。

`apply ... to` 按产物路径匹配。**不逐个 `import` + `use`**：37 处重复会变成 74 处，那是把问题换了个方向而已。

**编译器补的**：哪些产物命中了哪条规则、展开顺序、以及第八节那个并集。
**编译器拒绝的**：两个写覆盖相交的非空跨度——报错并指出是哪条规则的哪一行和哪条语句。

---

### 9.6 逃生舱

三处地方作者**必须**说话，因为编译器不该替他决定：

```
base."How it compares".drop(reason: "上游在和别的项目比，与我们无关")
base.replace(self, reason: "两份脚本会跑两遍")
skip skill
```

判据一致：**凡是会让保证量下降的，必须留下一句为什么**；而那句为什么会出现在构建报告里。`skip` 也一样——沉默地不套用规则是不行的，那正是这门语言从一开始就在防的东西。

---

### 9.7 地址是有类型的

每个地址都有类型：**（种类，部分类别）**。

| 种类 | 部分类别 |
|---|---|
| markdown | `section` `line` `frontmatter` `fmkey` `body` |
| shell | `function` `marker` `line` |
| toml / json | `key` `value` |
| text | `line` |

写操作要求**内容的类型与落点的类别相符**：一个 shell 函数写不进 markdown 的节，编译器在解析期就拒绝，而不是织出一坨东西再让人去发现。

```
base.Overview.after(self.boot)
                    ~~~~~~~~~ shell/function 写不进 markdown/section
```

`as(kind)` 是这个类型系统里**唯一的显式转换**：把一个值重新当作某种文档打开，类型从 `toml/value` 变成 `markdown/document`。有且只有一个转换算子，是刻意的——转换多了，"这段到底是什么" 就不再能静态回答。

---

### 9.8 模块

**一个 `.lm` 文件就是一个模块，它定义的规则就是它的导出。**

```
import std "/rules"

apply std.skill to "skills/**"
```

不设 `public` / `private`：一个文件里的规则本来就是给人用的，多一层可见性只是多一个要写的词。**需要私有时，把它拆到另一个文件里**——文件边界已经是边界了。

循环导入是错误，不是警告。

---

### 9.9 规则的组合与参数

**组合**：规则里可以展开别的规则。

```
rule skill {
  use frontmatter
  base.append(self.body)
}
```

展开是**纯替换**——`use` 之后仍然只有 `write(地址, 文本)`，第八节那条判据不变。循环展开是错误。

**参数**：

```
rule extend(sec) {
  base.Overview.after(sec)
}

apply extend("Position in SDLC") to "skills/sdlc/**"
```

参数的类型**由用法推断**——`sec` 落在 `markdown/section` 的位置上，它就是 `markdown/section`。用法之间不一致时报错。这是「复杂留给编译器」：作者不写类型标注，编译器负责算出来并在冲突时说清楚。

真实数据里那 37 处重复**一个参数都不需要**。所以参数是留好的位置，不是第一期要做的东西。

---

### 9.10 文档注释

```
/// 每个 skill 都套这条：描述双语拼接，正文接在上游之后。
rule skill { ... }
```

`///` 挂在规则上，出现在补全列表、悬停、以及 `lm list` 的输出里。

这是纯粹的开发体验项，成本接近零：编辑器那一套（补全 / 悬停 / 跳转 / 诊断）已经在了，只是今天没有东西可挂。

---

### 9.11 语言版本

```
// loom.om
loom "1.2"
```

一棵要被引用很多年的树，必须能说出自己是按哪一版语言写的。`registry` 设置的移除、`loom.lm` → `loom.om` 的改名，两次破坏性变更都只能靠错误信息补救——有了这一行，编译器可以直接说"这棵树写于 1.1，这些地方变了"。

---

### 9.12 规范形式

每样东西**只有一种写法**，由 `lm fmt` 保证。

规则不是新发明的——anchor completion 今天写回模板时已经在执行它们了：超过 100 字符或带路径的调用写成块形式、块形式一行一个参数、缩进四格。把这套规则从"写回时顺便做的事"提成一个命令即可。

一门语言有没有规范形式，是代码评审里争不争缩进的分界线。

---

### 9.13 刻意不要的

专业不等于功能多。下面每一项都是常见语言特性，都被排除，理由一致：**它们会让写集合无法界定，或者让"有没有东西悄悄变了"退回"跑一遍看看"。**

| 不要 | 为什么 |
|---|---|
| 表达式、算术、字符串拼接 | 算出来的文本不是一段具名范围 |
| 控制流（if / for） | 产物依赖求值路径，写集合不再静态可知 |
| 变量与赋值 | 同上；`{{@name}}` 是**替换**不是变量，它不参与寻址 |
| 继承 / 覆盖链 | 融合已经是集合并，重叠是错误——引入优先级等于把可检测的冲突变成沉默的行为 |
| 返回文本的宏 | 第八节：保证量归零，必须走 `merge` 那条路 |
| 一个文件多个产物 | 一份源对一个产物，是"这个产物由什么组成"能被回答的前提 |

**这张表和上面所有功能同等重要。** 一门语言的形状由它拒绝的东西决定，而这些拒绝全部来自同一条不变式，不是口味。

---

### 9.14 编译器补什么

| 作者不写 | 编译器算 | 今天 |
|---|---|---|
| 文档种类 | 扩展名 | ✅ |
| 部分的类别 | 种类的默认类别 | ✅ |
| 路径 | 名字唯一时不需要 | ✅ |
| 未被安放的内容该去哪 | 按它在我们文件里的邻居推断，**并写回模板** | ✅ anchor completion |
| 公共缩进 | 剥掉 | ✅ |
| 规则命中了谁 | glob | ✍️ 待做 |
| 写集合、重叠检测 | 由不变式那套机器算 | 部分 |
| 保证量 | 写集合之外的比例 | ✍️ 待做 |

### 9.15 编译器拒绝什么

| 情形 | 为什么不猜 |
|---|---|
| 名字匹配到两个部分 | 织错位置的产物和正确的长得一模一样 |
| 名字一个都没匹配到 | 同上 |
| 两个写跨度相交 | 结果取决于顺序，而顺序不该有意义 |
| 上游内容不见了且无人认领 | 这就是整门语言存在的理由 |
| `drop` / `replace` 没写理由 | 保证量下降必须留痕 |
| 未被安放的内容找不到邻居 | 没有可推断的依据时，推断就是猜 |

---

### 9.16 文法

```ebnf
file       = { comment | doc | import | rule | apply | skip | statement } ;

import     = "import" [ ident ] string ;
rule       = "rule" ident [ "(" ident { "," ident } ")" ] "{" { statement | use } "}" ;
use        = "use" qname ;
apply      = "apply" qname [ "(" arg { "," arg } ")" ] "to" string { string } ;
skip       = "skip" qname ;
qname      = ident [ "." ident ] ;                (* 模块.规则 *)

statement  = target ( "(" [ args ] ")" | ":" block ) ;
target     = root { "." step } [ "." place ] ;
root       = "base" | "self" | ident ;
step       = ident | string | kind-call | "as" "(" kind ")" ;
kind-call  = ( "section" | "line" | "key" | "path" | "function" | "marker" ) "(" string ")" ;
place      = "after" | "before" | "start" | "append"
           | "replace" | "drop" | "set" | "merge" ;

args       = arg { "," arg } | "{" { arg NEWLINE } "}" ;
arg        = string | target | ident | "reason" ":" string ;
block      = NEWLINE indented-lines ;             (* 缩进定界，内容原样，不转义 *)

doc        = "///" ... NEWLINE ;                  (* 挂在其后的 rule 上 *)
comment    = "//" ... NEWLINE ;
string     = '"' ... '"' ;                        (* 转义只有 \" 和 \\ *)
ident      = letter { letter | digit | "_" } ;    (* _ 匹配空格或下划线 *)
kind       = "markdown" | "shell" | "toml" | "json" | "text" | ... ;
```

整个文法里**没有表达式、没有控制流、没有变量、没有返回值**。规则只能展开成语句，语句只能是"在一个地址上写"。

一句话一行。这不是简陋，是第三节的直接后果：**系统里只有一个操作，文法就不该长出第二个。**

---

## 十、这套系统今天实现了多少

| 系统 | 今天 | 缺口 |
|---|---|---|
| 文档 = (种类, 文本) | ✅ | 五种类型，`ast.New` 是一条 switch |
| 部分可作为文档打开 | ✅ `as` | 只在 toml/json 的值上；markdown 代码块不行 |
| 地址 = 名字串 | ✅ | 根固定为 base/self/import，不能跨树 |
| 派生地址 | ✅ | —— |
| 单一 write | ✅ 八个动词都是它 | —— |
| 不变式 | ✅ 每次构建断言 | —— |
| 保证是量 | ⚠️ 分成三档，不是连续量 | 报告没给出"保证了多少" |
| 读被记录 | ❌ | 构建期算出来就扔了 |
| 身份 = 版本间关系 | ❌ | sync 合并内容，不合并地址空间 |
| 只说一次 | ❌ | 291 条语句里 68 条是四行样板 |

---

## 十一、分期

每期独立证明或证伪一件事：

| 期 | 做什么 | 证明什么 |
|---|---|---|
| 1 | 再加一种类型，并让它**两个方向都能嵌** | 递归那条规则是不是真的成立 |
| 2 | 把读记录下来 | 图是不是白送的 |
| 3 | 只说一次（`rule` / `apply`，第八节） | 抽象能否不动不变式——**融合那条已用真实数据验过** |
| 4 | sync 时算重命名 | 身份能否被声明而不是被猜 |

第 1 期排前面不是因为类型重要，而是**它验证第一节那条递归规则**——整套系统压在那条规则上。如果一种新类型没法两个方向都嵌，那么「一张布」就是假的，后面三期都得重想。

---

## 会推翻这套系统的问题

它压在一个判断上：**人们想对文档做的事，都能说成「在某个地址上写」**。

> **有没有一个你真想做、却没法表达成「命名一个部分，并在它或它的边界上写」的改动？**

有——从那里重新开始。
没有——那缺的只是上面那张表。
