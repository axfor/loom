# Loom 设计

> 把散落在各种类型和位置的**文档与数据**，组织成一张结构化、**可引用**的布。

下面从这句话推出需要的全部机器。不参照现有实现；实现放在最后，作为这套系统的一个实例。

---

## 一、两个数据概念

**文档** = （种类，文本）。
**部分** = （名字，跨度）。种类决定一份文档有哪些部分；名字是**作者写的**（标题、函数名、键名），不由我们发明。

一条递归规则把这两个概念变成一张布：

> **一个部分的内容，本身可以作为文档打开。**

一个 json 值里是一段 markdown，那段 markdown 有节；一个 markdown 代码块里是 shell，那段 shell 有函数。

所以「混杂」不是「很多种文件堆在一起」，是**种类套着种类**。这张布有深度。数据和文档在这里没有区别——json 是文档，它的值也是文档。

---

## 二、一个地址

**地址** = 从某份文档出发的一串名字。

```
d.a.b.c
```

因为部分可以作为文档打开，这串名字**自然跨种类**，不需要第二套语法。

### 派生地址

除了部分本身，四个位置也能被寻址——它们是部分边界上的**空跨度**：

```
d.x 之前   d.x 之后   d 的开头   d 的结尾
```

这一步是整个系统里最省事的一刀：**「在 X 之后插入」不是一个操作，是一个地址。**

---

## 三、一个操作

```
write(地址, 文本)
```

把地址指向的跨度，换成给定的文本。**没有第二个操作。**

今天语言里的八个动词，全部是它在不同地址上的实例：

| 写成 | 其实是 |
|---|---|
| `after(t)` / `before(t)` | 写到边界空跨度 |
| `start(t)` / `append(t)` | 写到文档首 / 尾的空跨度 |
| `replace(t)` | 写到该部分的跨度 |
| `drop()` | 往该部分的跨度写空 |
| `set(v)` | 写到值的跨度 |
| `merge(d)` | 写到**根**跨度 |

`as` 不是操作，是地址派生：把一个部分当作文档打开。

### 语法不用重做

值得注意的是：`base.Overview.after("...")` 里，**动词是地址的一部分，不是操作**。`base.Overview.after` 读作「Overview 之后那个空跨度」，括号里才是要写的文本。

也就是说今天的语法**已经在表达这套系统**了，只是从没这么描述过。把它改成 `base.Overview.after = "..."` 之类只是换个写法，不增加任何表达力。**这套系统要求的语法变化为零**——真正要加的只有抽象。

---

## 四、一个不变式

既然唯一的改动方式是「把某段跨度换掉」，那么：

> **写集合之外的每一个字节，与来源逐字节相同。**

这不需要检查，它是构造的结果。要验证也只需一步：**把写过的跨度抠掉，剩下的必须等于来源。**

标记（`<!-- MINE:BEGIN -->`）不是注释风格问题——**它就是写集合在产物里的记录**，让「抠掉」这件事在事后仍然可做。

### 保证是量，不是档

一个视图的可信程度 = **产物中不在写集合内的比例**。

| 写集合 | 上游被保证的部分 |
|---|---|
| 几个空跨度（纯插入） | 100% |
| 若干部分跨度（replace / drop） | 除这些部分外全部 |
| 根跨度（merge、整文件替换） | 0% |

所以 `merge` 不是"另一类东西"，它是写集合等于全文的那个极端。它必须写明理由、必须在报告里单列——不是因为它特殊，而是因为它的保证量是零。

**这条给了未来任何扩展一把尺子**：不问「允不允许」，问「它的写集合有多大，报告里说清楚了没有」。

---

## 五、引用与图

**读**一个地址 = 取出那份文档。引用就是被当作值使用的地址，它**不复制**。

写只发生在写集合，读发生在任何地方。于是：

> **把构建期发生过的读记录下来，依赖图就有了，不需要额外机制。**

「谁引用了这一节」「它移走会炸掉什么」「这个产物由什么组成」——都是这张记录的反向查询。这不是新功能，是**不把已经算出来的东西扔掉**。

---

## 六、身份

名字来自文本。文本变了，名字就变了。

所以**身份不是一份文档的属性，是两个版本之间的一个关系**。

这条推论决定了它只能住在哪里：**只有同时握有新旧两版的那一刻，才能判断「内容没变而标题变了 = 重命名」而不是「删除 + 新增」。**

于是三条路直接出局：

| 方案 | 为什么不行 |
|---|---|
| 给上游加 id | 上游不可碰是前提 |
| 内容哈希 | 把「改标题就断」换成「改内容就断」 |
| 模糊匹配 | 猜错的产物和正确的长得一模一样 |

唯一的位置是**取上游新版的那一刻**。那时把重命名算出来、记下来、**报告出来**——引用跟随是被声明的，不是被猜的。

---

## 七、种类

一个种类要进这张布，回答三个问题：

1. 它把文本切成哪些**有名字的部分**？
2. 它的部分能被当作文档**打开**吗？（往下嵌）
3. 它能作为别人的部分**被打开**吗？（被嵌）

只答第 1 问的种类只做了三分之一——它只能当顶层文件，进不了那张有深度的布。

---

## 八、抽象与融合

### 判据由系统给出

一条规则要能只说一次。什么样的抽象是安全的，不需要争论：

> **展开之后，是不是仍然只有 `write(地址, 文本)`？**

这等价于说：**抽象的返回类型是「一组写」，不是「一段文本」。**

返回文本的函数（`func merge(){ return a.call(...) }`）不是被禁止，是它的写集合无法界定——等价于写了根跨度，保证量归零。它必须和 `merge` 走同一条路：写明理由、报告单列。**不是禁止计算，是计算的代价在报告上标价。**

### 全部在 `*.lm` 里

规则用语言自己写，不进设置文件——设置是一行一条，装不下块，而语言的东西该待在语言的文件里。

```
// mine/rules.lm —— 定义规则，自己不产出任何文件
rule skill {
  base.frontmatter.description.start(self.frontmatter.description)
  base.append(self.body)
}

apply skill to "skills/**"
```

两点设计：

**规则体就是语句本身。** 不发明新语法——`rule` 块里写的和模板里写的是同一种东西，因为展开后必须是同一种东西。

**按模式套用，不是逐个引用。** 如果每个模板都要写 `import` + `use`，37 处重复就变成 74 处。`apply ... to` 让那 24 个「除了样板没有一句自己的话」的模板**整个消失**。

一个只有 `rule` / `apply`、不指向任何产物的 `.lm`，就是规则文件；构建报告里单列一行说它套用到了哪些产物。

### 融合：是并集，不是优先级

这是整套系统最省事的一处，而且是白送的。

因为**所有地址都对着来源解析**，编辑收集齐了再统一应用，所以：

> **规则的写 和 模板的写，融合方式就是集合并。**

不需要定义谁覆盖谁、谁先谁后——不相交的写**可交换**。只有两种情况需要说清：

| 情形 | 语义 |
|---|---|
| 两个写落在**同一个空跨度**（都往 X 之后插） | 都生效，按顺序排：规则在前，模板在后 |
| 两个写覆盖**相交的非空跨度**（一个 replace 一个 drop） | **冲突，报错**，并指出是哪条规则和哪条语句 |

所以「怎么融合」在这套系统里不是一个需要设计优先级的问题，而是一个**可检测的重叠问题**——而检测重叠用的正是维护不变式的同一套机器。

### 拿真实数据验过

把上面那条 `rule skill` 套到唯一使用 Loom 的项目的 81 个模板上：

| 结果 | 数 |
|---|---|
| 命中规则的模板 | 37 |
| 套用后**整个文件消失**（没有剩下任何自己的话） | **16** |
| 套用后只剩 1–2 条 | 14 |
| **真正的重叠冲突** | **0** |

「融合是并集」这条在 81 个真实模板上**一个反例都没有**。

这不等于它永远不会冲突。它说明的是：**冲突是罕见的例外，所以值得报错，而不值得为它设计一套优先级规则**——而这正是上面那张表假设的东西。设计在写代码之前就被真实数据验过一次。

### 退出

个别文件不想要某条规则时，必须**明说**，否则就是上面那个重叠错误：

```
skip skill
```

明说了就允许，并在报告里记一笔。沉默地不生效是不行的——那正是这门语言从一开始就在防的东西。

## 九、这套系统今天实现了多少

| 系统 | 今天 | 缺口 |
|---|---|---|
| 文档 = (种类, 文本) | ✅ | 五种类型，`ast.New` 是一条 switch |
| 部分可作为文档打开 | ✅ `as` | 只在 toml/json 的值上；markdown 代码块不行 |
| 地址 = 名字串 | ✅ | 根固定为 base/self/import，不能跨树 |
| 派生地址 | ✅ | —— |
| 单一 write | ✅ 八个动词都是它 | —— |
| 不变式 | ✅ 每次构建断言 | —— |
| 保证是量 | ⚠️ 分成三档，不是连续量 | 报告没给出"保证了多少" |
| 读被记录 | ❌ | 构建期算出来就扔了 |
| 身份 = 版本间关系 | ❌ | sync 合并内容，不合并地址空间 |
| 只说一次 | ❌ | 291 条语句里 68 条是四行样板 |

---

## 十、分期

每期独立证明或证伪一件事：

| 期 | 做什么 | 证明什么 |
|---|---|---|
| 1 | 再加一种类型，并让它**两个方向都能嵌** | 递归那条规则是不是真的成立 |
| 2 | 把读记录下来 | 图是不是白送的 |
| 3 | 只说一次（`rule` / `apply`，第八节） | 抽象能否不动不变式——**融合那条已用真实数据验过** |
| 4 | sync 时算重命名 | 身份能否被声明而不是被猜 |

第 1 期排前面不是因为类型重要，而是**它验证第一节那条递归规则**——整套系统压在那条规则上。如果一种新类型没法两个方向都嵌，那么「一张布」就是假的，后面三期都得重想。

---

## 会推翻这套系统的问题

它压在一个判断上：**人们想对文档做的事，都能说成「在某个地址上写」**。

> **有没有一个你真想做、却没法表达成「命名一个部分，并在它或它的边界上写」的改动？**

有——从那里重新开始。
没有——那缺的只是上面那张表。
