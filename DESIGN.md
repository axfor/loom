# Loom 设计

一份系统性的设计笔记，写于决定这门语言该走向哪里的时候。README 讲它今天做什么；这份讲它是什么、为什么这样、以及下一步该动哪里。

---

## 论点

> 把散落在各种类型和位置的**文档与数据**，组织成一张结构化、**可引用**的布。

把上游层和我们自己的层织成产物，是这张布存在之后能做的一件事。那不是目的，是第一个用法。

三个词各自扛着分量。

**散落。** Markdown、shell、toml、json、yaml、csv、源码——一套 skill、一棵配置树、一份文档集，同时是这些东西。而碰它们的每个工具都把每种类型当成独立的世界：各自的编辑器、各自的打补丁方式、各自出错的方式。

**结构化。** 文档不是字符串。它是一组**有名字的部分**：一个标题和它底下的内容、一个函数、一个键、一个 frontmatter 字段。这些名字**作者已经写好了**，不需要发明——只需要捡起来。

**可引用。** 这一条才让它成为一张布而不是一堆纸。如果每份文档的每个部分都能被命名，那么一份文档可以**指向**另一份的某个部分而不是复制它；一个产物可以由多个来源装配；「什么依赖了这一节」才有答案。

---

## 一、模型

五个概念，互相定义：

| 概念 | 是什么 |
|---|---|
| **文档** | 一段文本 + 一个种类。种类决定它有哪些部分 |
| **部分** | 文档里一段**有名字**的范围。名字是作者写的（标题、函数名、键名） |
| **地址** | 从某个根出发的一串名字：`base."Example 2"."Phase 1"` |
| **引用** | 被当作值使用的地址。引用**不复制**内容 |
| **视图** | 由若干文档经操作派生出的文档。产物就是一个视图 |

### 种类是嵌套的

这是最容易被忽略、也最关键的一点。`as` 算子把一个部分的**值**重新当作另一种文档打开：

```
base.prompt.as(markdown).Steps.after("我们的步骤")
```

一个 toml 键的值，里面是 markdown，于是它有节、有标题、能被继续寻址。

所以「混杂」不是「很多种文件放在一起」，而是**种类套着种类**：yaml 里嵌 markdown、json 里嵌 shell、markdown 代码块里嵌任何东西。这张布是有深度的，而 `as` 是往下走一层的算子。

**设计含义**：任何关于「支持哪些类型」的讨论，都必须同时回答「它能被嵌进别的类型里吗」「别的类型能嵌进它吗」。一个只能当顶层文件的类型，只做了一半。

### 部分是平的，地址是有层级的

今天一个 markdown 节是「标题 → 下一个任意级别的标题」，**不包含**子节；但地址 `base."A"."B"` 会在 A 的整章范围内查找 B。

即：**范围是平的，寻址是树状的。** 这个不一致是有意的（节要能被单独替换而不牵连子节），但它也是 `drop` 一个有子节的节时必须逐个交代子节的原因。

**待定**：这张布长大以后，部分该不该真的成树？成树则「整章」有了名字，代价是每个操作都要回答「作用于节点还是整棵子树」。

---

## 二、地址

### 今天能写的

```
base.Overview                          上游文档的一个节
self."Example 2"."Phase 1"             路径：在某章内定位
base.frontmatter.description           一个 frontmatter 键的值
cmd.body                               另一份我们文件的正文
base.prompt.as(markdown).Steps         嵌套：值里的文档的节
```

已经相当完整：**跨文档引用存在**（`import` + 名字），**跨种类引用存在**（`as`）。

### 缺的

**根是固定的。** 只有 `base`（上游）、`self`（我们）、`import` 进来的我们层的文件。无法指向：另一棵树、另一个仓库、产物自身。

**引用是单向的、且构建后蒸发。** 这张布知道「模板 X 引用了标题 Y」，但答不出：谁引用了 Y、Y 移走会炸掉什么、这个产物由什么组成——除非重跑一次构建。

**没有地址值。** 地址只能出现在语句里，不能被存下来、传递、比较。

---

## 三、身份（最难的一条）

线是系在**作者写的字**上的：节的地址是标题文字，函数的地址是函数名。上游改个标题，指向它的所有引用全断。

今天这件事**大声失败**（构建停住，不猜）。对一次构建这是对的答案；对一张要被引用很多年的布是错的。

四个选项，三个不成立：

| 方案 | 为什么不行 |
|---|---|
| 给上游加显式 id | **上游不可碰是整个语言的前提。** 直接出局 |
| 内容哈希匹配 | 改了内容就断，和改了标题就断一样脆 |
| 模糊匹配 / 最相似 | 「不猜」是核心承诺。织错位置的产物和正确的长得一模一样 |
| **在 sync 时刻重建映射** | ✅ 见下 |

**唯一可行解：`lm sync` 是同时握有新旧两版上游的唯一时刻。**

那一刻可以判断：一个节的**内容没变而标题变了** → 这是重命名，不是删除。把它记进一份映射文件，引用就能跟着走，并且这件事是**被报告出来的**（「上游把 X 重命名为 Y，3 处引用已跟随」），而不是被猜出来的。

这不是新机制，是把 sync 已经在做的三方合并推广到**名字**上。今天 sync 合并的是文件内容；它同样可以合并**地址空间**。

---

## 四、操作

每个操作都是：**把名字解析成范围，在它周围拼接。**

| 类 | 操作 |
|---|---|
| 放置 | `after` `before` `start` `append` |
| 改写 | `replace` `set` |
| 移除 | `drop` |
| 整体 | `merge` |
| 开视图 | `as` |

**这个集合是封闭的，而封闭本身就是保证**（见下一节）。加操作之前唯一要问的是：它能不能表达成「名字 → 范围 → 拼接」。能，就安全；不能，就得进隔离区。

---

## 五、保证

### 不变式

> **一个视图不可能悄悄丢掉它没有命名的东西。**

因为没有任何操作能碰到没被命名的范围，所以「有没有什么没被命名的东西变了」是**可判定的**——不是靠构建完再 diff，而是由构造决定。

今天 `lm build` 用字节比对断言它。这张布将来长成什么样，**要守住的都是这一句**。

### 三档

| 档 | 含义 | 今天有谁 |
|---|---|---|
| **可验证** | 插入式：剥掉标记后必须与上游逐字节相同 | 绝大多数模板 |
| **隔离** | 无法验证，**必须写明理由**，构建报告里单列 | `merge`、整文件 `replace` |
| **禁止** | 会让不变式失效且无法隔离的 | 返回计算文本的函数 |

关键不是「禁止一切不可验证的东西」，而是**不可验证的东西必须在报告里可见**。今天报告已经分 `extended` / `overridden` 两类，这个结构接得住未来的扩展。

---

## 六、类型

### 一个种类必须提供什么

今天的契约只有七个方法：把名字解析成行范围、替换行范围，外加枚举与取值。

要成为这张布的一等公民，一个种类还需要回答两个问题：**它能被 `as` 嵌进别的类型吗**（值 → 文档），**别的类型能被嵌进它吗**。

### 成本

| 类型 | 行数 |
|---|---|
| text | 27 |
| shell | 93 |
| toml | 99 |
| markdown | 111 |
| json | 207 + 220（registry 合并需要真解析器） |

**约一百行开启一整类文档。** 限制「一切文档」的是一条 switch 语句，不是设计。

---

## 七、抽象

唯一使用 Loom 的项目里：81 个模板、291 条语句，其中 **68 条是四行逐字相同的样板（23%）**，**24 个模板里没有一句是它自己特有的**，56% 的模板不超过两条语句。

没有任何机制能把一条规则只说一次。

两种抽象，结果完全不同：

**展开式**——`func` 发出 `after` / `append`，展开后仍是那个封闭集合。**保证按构造保留**。这条能吃掉 23%。

**计算式**——`func` 返回算出来的文本。危险不在于计算，而在于**算出来的字符串不是一段具名范围**，它是唯一能逃出封闭集合的形状。加了它，「有没有东西悄悄变了」就退回「跑一遍看看」。

若要加，它属于**隔离档**，和 `merge` 同级：写明理由、报告单列、永不与已验证的文件混在一起。

---

## 八、演进

`loom.om` 里没有语言版本声明。`registry` 设置的移除、`loom.lm` → `loom.om` 的改名，两次破坏性变更都只能靠错误信息补救。

一张要被引用很多年的布，需要能说出「这棵树是按哪一版语言写的」。

---

## 九、分期

每一期都该能**独立证明或证伪**一件事：

| 期 | 做什么 | 证明什么 | 代价 |
|---|---|---|---|
| **1** | 再加一种类型（yaml） | 「一切文档」是不是一百行一个的事 | 约 100 行，零风险 |
| **2** | 说一次的能力（默认值 → 展开式 `func`） | 抽象能否在不动保证的前提下吃掉样板 | 中，保证不变 |
| **3** | sync 时刻重建身份 | 引用能否扛住上游重命名 | 大，是真设计问题 |
| **4** | 反向查询（谁引用了它） | 这张布能否被提问，而不只是被构建 | 大，需要持久化地址空间 |

**第 1 期先做**，不是因为 yaml 特别，而是它验证的是最底层的假设，而且做完你就确切知道后面几期站在什么地基上。

---

## 决定下一步的那个问题

整个论证压在一个尚未验证的判断上：**人们想对文档做的事，都能说成「命名一个部分，并相对它放置些什么」**。这是可证伪的，值得在画任何语法之前先验。

> **有没有一个你真的想做、却没法表达成「命名文档的某个部分」的改动？**

如果有——设计就从那里开始，它比任何语法草案都值钱。
如果没有——那缺的东西比看上去窄得多，而该动手的顺序就是上面那张表。
